<h1>
	<%= @user.name.downcase %>
</h1>
    <% if @user.debates.any? %>
      <h2>
      	my <%= pluralize(@user.debates.count, "debate") %>
      </h2>
      <ol class="debates">
        <%= render @debates %>
      </ol>
      <%= will_paginate @debates %>
    <% end %>

<% if @feed_items.any? %>
  <ol class="debates">
    <%= render partial: 'shared/feed_item', collection: @feed_items %>
  </ol>
  <%= will_paginate @feed_items, page_links: false, class: "my_pagination" %>
<% end %>
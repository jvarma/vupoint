
  <% unless debate.viewpoints.empty? %>
    <div class="row-fluid">
      <div class="content win-position">
        <%=  image_tag("thumbs_up.png") %> <%= debate.viewpoints.first.desc  %>
      </div>
    </div>
  <% end %>

  <% unless (debate.viewpoints.empty? || debate.viewpoints.size == 1) %>
    <div class="row-fluid">
      <div class="content loss-position">
        <%=  image_tag("thumbs_down.png") %> <%= debate.viewpoints.last.desc  %>
      </div>
    </div>
  <% end %>